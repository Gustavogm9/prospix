# Matriz Inicial de Achados

Status da auditoria inicial: preliminar. Nao expor segredos reais neste arquivo.

| ID | Achado | Squad | Severidade | Prioridade | Confianca | Status | Evidencia | Recomendacao | Criterio de aceite |
|---|---|---|---|---|---|---|---|---|---|
| AUD-P0-001 | RLS podia nao manter `app.tenant_id` durante queries comuns quando contexto tenant nao seguia corretamente o lifecycle e/ou nao havia transacao controlada. | 1 | Critica | P0 | Media | Resolvido local DB-backed | `apps/api/src/lib/prisma.ts` usa `set_config(..., true)` parametrizado dentro de `$transaction`; `tenant-context.ts` usa `enterWith`; `01_rls.sql` agora e idempotente, cria usuario `prospix_app` nao-superuser para prova real e `rls-sql-hardening.test.ts` passou; em 23/05/2026 `AUDIT_REQUIRE_DB=1` passou com 5/5 e 0 skips | Reproduzir no CI e ampliar cobertura DB-backed para outras tabelas criticas. | Teste multi-tenant prova tenant vazio, tenant A, tenant B e escrita cross-tenant bloqueada com Postgres real. |
| AUD-P0-002 | Suite agregada `npm test` nao estava verde; `@prospix/shared-types` rodava Vitest sem arquivos de teste. | 5 | Alta | P0 | Alta | Resolvido | `npm test` passou em 22/05/2026 com 32 arquivos e 162 testes; `packages/shared-types/package.json` usa `--passWithNoTests` | Manter `test:unit` sem dependencia de DB/Redis obrigatorios. | `npm test` passa sem falso negativo. |
| AUD-P0-003 | Teste RLS/multi-tenant exige Postgres local e nao prova isolamento fora de ambiente preparado. | 1 | Critica | P0 | Alta | Resolvido local DB-backed | Docker/Postgres/Redis foram provisionados; banco `prospix_test` recebeu migrations, RLS e seed; em 23/05/2026 `AUDIT_REQUIRE_DB=1` passou com 2 arquivos/5 testes/0 skipped/todo usando `prospix_app` nao-superuser; JSON `multi-tenant-results.json` confirmou sucesso | Reproduzir no CI e manter falha obrigatoria se houver skip/todo. | `npm run test:multi-tenant` passa com DB provisionado e sem skips. |
| AUD-P1-004 | `SET ROLE guilds_admin` usava pool Prisma e dependia de `RESET ROLE` em `onSend`, com risco de estado persistente em conexao. | 1 | Alta | P1 | Media | Resolvido local DB-backed | `apps/api/src/routes/admin/index.ts` remove hooks globais e usa `SET LOCAL ROLE guilds_admin` dentro de `$transaction`; `admin-role-leak.test.ts` passou em 23/05/2026 com Postgres real, role/grants aplicados e 0 skips | Reproduzir no CI e cobrir request admin HTTP real alem do teste DB direto. | Teste prova que request tenant apos admin nao herda role/bypass. |
| AUD-P1-005 | CORS podia aceitar wildcard em contexto inadequado para producao autenticada. | 2 | Alta | P1 | Alta | Resolvido | `apps/api/src/index.ts` usa allowlist deduplicada em producao e wildcard apenas fora de producao; `api-hardening.test.ts` passou para origem permitida, origem negada e dev wildcard | Manter origens por env e negar origem desconhecida em producao. | Config mostra origens permitidas por env e wildcard apenas em dev/test. |
| AUD-P1-006 | Error handler retornava `error.message` para cliente em erro interno, podendo vazar detalhe tecnico. | 2 | Alta | P1 | Alta | Resolvido | `apps/api/src/index.ts` registra error handler antes das rotas e usa `getPublicErrorMessage`; `api-hardening.test.ts` passou para erro 500 em rota plugin sem vazar mensagem interna | Manter teste dedicado para erro 500 em producao. | Erro 500 retorna mensagem generica sem stack/detalhe interno. |
| AUD-P1-007 | Busca indicou logs com `messageContent`, `body`, erros externos e possiveis payloads sensiveis. | 3 | Alta | P1 | Media | Em mitigacao | `apps/api/src/lib/logger.ts` amplia redaction para `rawBody`, `body` e `messageContent`; guardrails nao logam mais a resposta rejeitada em `msg`, apenas `outputLength`; AI router usa `AIProviderError` sem propagar corpo bruto do provider em `err.message`; `prompt-validation.test.ts` cobre que mensagem rejeitada nao aparece em logs; `npm test` passou em 22/05/2026 com 32 arquivos/162 testes. | Revisar mensagens literais remanescentes, `err.message` e stacks antes de go-live. | Testes/log snapshots nao exibem texto completo de lead, token, body cru ou segredo. |
| AUD-P1-008 | `executeRawUnsafe` interpolava valores em RLS/admin helpers. | 1 | Alta | P1 | Alta | Resolvido | `rg executeRawUnsafe apps/api/src` nao encontra uso; admin usa `$executeRaw` parametrizado/estatico | Manter regra de review reforcado para SQL raw. | Grep nao encontra interpolacao insegura em SQL raw sensivel. |
| AUD-P2-009 | `docs/agents/README.md` ainda registrava implementacao como nao iniciada apesar de codigo real existir. | 5 | Media | P2 | Alta | Resolvido | `docs/agents/README.md` | Atualizar estado atual e incluir auditoria oficial. | Documento reflete baseline atual. |
| AUD-P2-010 | `docs/dev-plan.md` ainda continha partes sem a camada Codex Auditor Oficial. | 5 | Media | P2 | Alta | Resolvido | `docs/dev-plan.md` | Inserir papel do auditor, gates e squads de auditoria. | Documento aponta para `docs/auditoria`. |
| AUD-P0-011 | Auth nao existia montado em `/v1`, mas OpenAPI, web e admin chamavam `/v1/auth/*`. | 2 | Critica | P0 | Alta | Resolvido | `apps/api/src/index.ts` monta `authRoutes` tambem em `/v1/auth`; `auth-versioned.test.ts` passou para `/v1/auth/magic-link`, `/v1/auth/refresh` e compatibilidade `/auth/magic-link`; typecheck API passou | Manter teste de contrato versionado e expandir para callback/admin-login. | `/v1/auth/*` critico nao retorna 404 e nao exige tenant auth. |
| AUD-P1-012 | Rotas de conversations/scripts existiam em arquivo tenant legado, mas nao eram montadas no router tenant atual. | 2 | Alta | P1 | Alta | Resolvido | `apps/api/src/routes/tenant/index.ts` registra `tenantContractRoutes`; `tenant-contract.test.ts` passou para conversations e scripts sob `/v1/tenant`; typecheck API passou | Manter contrato montado e migrar arquivo legado para modulos dedicados em refactor futuro. | Rotas documentadas de conversations/scripts nao retornam 404 sob `/v1/tenant`; drifts remanescentes ficam em `AUD-P1-014`. |
| AUD-P1-013 | Envelopes e status codes reais divergem de OpenAPI/shared-types. | 2 | Alta | P1 | Alta | Em mitigacao | `CRITICAL_API_CONTRACTS` inclui conversations, scripts, leads, dashboard, meetings, notification preferences, integrations, billing/admin, webhooks, auth e admin criticos; OpenAPI marca response/error shape para esses recortes; `tenant-contract.test.ts`, `auth-admin-contract.test.ts` e `webhooks.test.ts` validam contratos criticos sem DB; suite focada passou com 10 testes em 22/05/2026. | Expandir o gate para campaigns, dashboard/performance e envelopes completos de erro/sucesso, incluindo callbacks externos nao modelados como contrato critico. | Testes de contrato validam sucesso/erro para endpoints criticos. |
| AUD-P1-014 | Frontend chama paths tenant incorretos ou inexistentes. | 2/4 | Alta | P1 | Alta | Em mitigacao | Web passou a usar `/tenant/dashboard/today`, `/tenant/leads/:id`, `/tenant/meetings/:id`, `/tenant/scripts/simulate`; API ganhou `POST /v1/tenant/scripts` e `POST /v1/tenant/scripts/simulate`; contratos OpenAPI/shared-types de meetings, notification preferences, integrations, billing/admin e webhooks agora refletem rotas reais; OpenAPI removeu rotas fantasmas de `/tenant/me`, `/tenant/lgpd/*`, `/tenant/leads/{id}/assume|release`, `/tenant/scripts/templates` e `/webhooks/google/calendar`; typechecks e suites focadas passaram. | Rodar smoke web pos-login e cobrir callback Google/fluxos visuais de integrations no app. | Smoke web pos-login nao tem 404 esperado em fluxo principal. |
| AUD-P1-015 | Contrato do webhook Asaas usava header diferente do backend. | 2/3 | Alta | P1 | Alta | Resolvido | OpenAPI exige `asaas-access-token`; backend agora prefere `asaas-access-token`, aceita `asaas-token` apenas como legado e nao loga token bruto em tentativa invalida; `webhooks.test.ts` passou com o header documentado. | Manter compatibilidade legada temporaria e remover `asaas-token` apos janela operacional definida. | Webhook Asaas documentado passa com assinatura/token esperado. |
| AUD-P2-016 | `shared-types` nao e gerado nem validado contra OpenAPI. | 2/5 | Media | P2 | Alta | Resolvido | `packages/shared-types/src/openapi.generated.ts` agora e gerado por `openapi-typescript@7.13.0` (3296 linhas); `pnpm --filter @prospix/shared-types gen:openapi` regenera; `pnpm --filter @prospix/shared-types verify:openapi` falha exit 1 se houver drift em um temp file; CI workflow `openapi-drift` (`.github/workflows/ci.yml`) bloqueia `build` se drift; `api.ts` re-exporta `ApiPaths`, `ApiComponents`, `ApiOperations`, `ApiWebhooks` e helper `Schema<K>`; typecheck `@prospix/shared-types` passou local apos refactor. | Considerar gerar Zod schemas para validacao runtime no boundary alem dos tipos TS. | Drift OpenAPI/shared-types falha no CI. ✅ |
| AUD-P1-017 | Refresh token era usado como `jti` do JWT e armazenado em claro; fluxo admin tambem precisava provar sessao com RLS ativo. | 1 | Alta | P1 | Alta | Em mitigacao avancada | `apps/api/src/services/auth-service.ts` agora salva `hashOpaqueToken(refreshToken)`, retorna `accessTokenId` e usa `withAuthRlsBypass()` com `SET LOCAL ROLE guilds_admin` dentro de `$transaction` para operacoes auth que precisam atravessar RLS sem tenant; `apps/api/src/routes/auth/index.ts` usa `accessTokenId` como `jti` e lookup admin via bypass DB-role transacional; `auth-session-hardening.test.ts` cobre create/rotate/revoke por hash e blacklist do `jti` separado; em 23/05/2026 `auth-admin-login.rls-db.test.ts` passou com `prospix_app` e RLS ativo criando sessao admin sem expor tokens. | Validar expiracao, logout e reuso negado com DB/Redis reais; considerar pepper/segredo de aplicacao para hash de token se o modelo de ameaca exigir protecao contra dump offline. | Testes cobrem refresh, logout, revogacao, expiracao e reuso negado em ambiente integrado. |
| AUD-P1-018 | Policy RLS de `users` permite linhas `GUILDS_ADMIN` fora do isolamento tenant. | 1 | Alta | P1 | Alta | Em mitigacao | `01_rls.sql` removeu `OR role = 'GUILDS_ADMIN'` da policy `users`; `rls-sql-hardening.test.ts` garante que o atalho nao volta. | Provar em Postgres real que tenant comum nao enxerga admin e que admin usa role DB controlada. | Teste DB-backed prova tenant comum nao enxerga admin. |
| AUD-P2-019 | Endpoint admin retornava `tenant.secret` completo. | 1/2 | Media | P2 | Media | Resolvido | Detalhe admin transforma `secret` em `credentialState` com flags/metadados seguros; `admin.test.ts` garante ausencia de `secret`, `apiKey`, `clientSecret`, `encrypted` e valores sensiveis mockados; `npm test` passou em 22/05/2026 com 32 arquivos/162 testes e `git diff --check` passou sem erros de whitespace. | Manter responses admin sem material criptografado e revisar novos campos de credencial por allowlist. | Resposta admin nao contem material criptografado desnecessario. |
| AUD-P1-020 | Workers criticos existem, mas parte deles nao e iniciada/consumida no bootstrap. | 3 | Alta | P1 | Alta | Em mitigacao | Worker index registra `capture-google-maps`, `enrich-leads`, `daily-digest`, `usage-aggregation` e `send-notification`; `buildTenantScheduledJobs`/`scheduleRecurringTenantJobs` adicionam schedulers tenant-scoped para digest/usage; `send-notification.test.ts`, `daily-digest.test.ts`, `usage-aggregation.test.ts` e `workers/index.test.ts` passaram dentro de `npm test` em 22/05/2026. | Validar processo worker/deploy com Redis real, schedulers ativos e idempotencia operacional. | Todas as filas possuem consumidor e produtor/scheduler auditados em ambiente de teste/deploy. |
| AUD-P1-021 | DLQ e nominal, sem fluxo operacional de dead-letter, alerta ou reprocessamento. | 3/5 | Alta | P1 | Alta | Em mitigacao avancada | DLQ fisica habilitada em `apps/api/src/lib/dlq.ts`: fila paralela `dlq-<worker>` por worker, enqueue idempotente por `source_job_id`, retencao 30d, snapshot completo (payload, tenant_id, trace_id, attempts, failed_reason); `observeQueueFailures()` em `queue.ts` chama `handleFailedExhausted()` automaticamente; allowlist `DLQ_REPLAYABLE_WORKERS` segue runbook (apenas `health-check` aprovado); endpoints admin `GET /v1/admin/dlq`, `GET /v1/admin/dlq/:worker`, `POST /v1/admin/dlq/:worker/:dlqJobId/replay` (com `dry_run`), `DELETE /v1/admin/dlq/:worker/:dlqJobId` exigem `approved_by` + `reason`; workers fora da allowlist retornam 403 com link ao runbook; OpenAPI documenta os 4 endpoints; `openapi.generated.ts` regenerado sem drift; `dlq.test.ts` cobre builders + allowlist + errors; `dlq-routes.test.ts` cobre validacao Zod + 403 fora da allowlist + 404 + dry-run; eventos `queue:dlq-enqueued`, `queue:dlq-replayed`, `queue:dlq-replay-dry-run`, `queue:dlq-replay-blocked-by-allowlist`, `queue:dlq-purged` registrados em logger sem PII. | Reproduzir prova Redis-backed (`failed-exhausted` enfileira DLQ); conectar alerta Sentry/Slack ao `queue:dlq-enqueued`; ampliar allowlist conforme idempotencia de cada worker for provada com Redis real. | Falha esgotada gera evento auditavel + DLQ fisica + replay manual com allowlist + audit log. |
| AUD-P1-022 | Idempotencia de webhook/fila permite duplicidade antes do worker persistir mensagem. | 3 | Alta | P1 | Alta | Resolvido local DB/Redis-backed | Evolution usa `createExternalEventJobId(tenantId, event, messageId)` ao enfileirar inbound; lookup de segredo Evolution usa `SET LOCAL ROLE guilds_admin` dentro de transacao para leitura admin-scoped com RLS ativo; Asaas tambem possui `jobId` deterministico; envios WhatsApp usam `createSendWhatsappJobId` e `createRescheduledSendWhatsappJobId`; `process-inbound.ts` faz dedupe por `whatsappMessageId`, trata `P2002`, diferencia retry por `attemptsMade`, grava inbound + contador em `$transaction` e usa `optout.upsert`; em 23/05/2026 `evolution-idempotency.redis-db.test.ts` passou com Postgres+Redis reais: dois webhooks concorrentes com o mesmo `messageId` produziram um unico job BullMQ e uma unica mensagem inbound com `messageCount = 1`; suite DB/Redis focada passou com 4 arquivos/11 testes. | Reproduzir no CI com Postgres/Redis provisionados e ampliar para Asaas/WhatsApp E2E conforme prioridade. | Dois webhooks identicos geram um unico efeito persistido. |
| AUD-P1-023 | Confirmacao de opt-out podia ser bloqueada pelo proprio worker de envio. | 3/6 | Alta | P1 | Alta | Resolvido | `process-inbound.ts` enfileira confirmacao com `force_send_optout_confirmation`; `send-messages.ts` preserva bloqueio normal e permite confirmacao explicita; `send-messages.test.ts` passou 2 testes | Manter bypass restrito apenas para confirmacao transacional de opt-out. | Opt-out confirma uma vez e bloqueia campanhas futuras. |
| AUD-P2-024 | Webhook Evolution aceita assinatura apenas se segredo estiver configurado. | 3/1 | Media | P2 | Media | Resolvido | `apps/api/src/routes/webhooks/evolution.ts` rejeita Evolution em `NODE_ENV=production` quando a instancia nao tem `evolutionWebhookSecret` e valida HMAC quando ha segredo; `webhooks.test.ts` cobre rejeicao 401 sem segredo em producao e passou em 22/05/2026. | Garantir configuracao de segredo por instancia antes de staging/producao. | Webhook sem HMAC valido e rejeitado em producao. |
| AUD-P2-025 | Custos de IA eram rastreados, mas nao bloqueavam uso em tempo real. | 3/6 | Media | P2 | Media | Em mitigacao | `apps/api/src/ai/quota.ts` centraliza limites por plano, estimativa de custo e `AIQuotaExceededError`; `AIRouter.call` chama `assertAIQuotaBeforeCall` antes de buscar segredos/provider; `callAIWithGuardrails` escalona `ai_quota_exceeded` sem retry pago; `usage-aggregation` e dashboard usam a mesma tabela de limites; `ai-quota.test.ts` e `prompt-validation.test.ts` passaram em 22/05/2026. | Validar com uso real/staging, definir politica comercial para compra de creditos/upgrade e revisar custo estimado por modelo. | Tenant acima do limite nao dispara nova chamada paga sem decisao explicita. |
| AUD-P2-026 | Campo `to` pode expor WhatsApp em logs de envio. | 3 | Media | P2 | Media | Resolvido | `send-messages.ts` loga `recipient` mascarado em vez de `to`; `send-messages.test.ts` garante que telefone cru e campo `to` nao aparecem. | Manter snapshots/redaction para logs de envio. | Logs nao exibem telefone de lead em claro. |
| AUD-P1-027 | Admin simula sucesso de criacao de tenant quando API falha. | 4/6 | Alta | P1 | Alta | Resolvido | `NewTenant.tsx` removeu convite fake e falha fechado com `toast.error`; typecheck admin passou. | Manter mocks apenas em dev/demo explicito. | Falha de API nao cria sucesso visual nem convite invalido. |
| AUD-P1-028 | Telas de producao trocam erro/ausencia de dados por mocks realistas. | 4 | Alta | P1 | Alta | Em mitigacao | `demo-mode.ts` centraliza permissao de mocks em DEV/flags demo; Home, Conversations, Leads, Pipeline, Schedule, Scripts e Settings foram condicionados; Settings nao exibe Pix/faturas/chave fake fora de dev/demo; typecheck web passou. | Completar estados estruturais de erro/vazio, landing submit sem falso sucesso e smoke browser pos-login. | Ambiente producao nunca executa acao sobre dados ficticios. |
| AUD-P1-029 | Links de Termos/Privacidade no cadastro web apontam para rotas inexistentes no SPA. | 4/6 | Alta | P1 | Alta | Resolvido | App web ganhou rotas publicas `/termos` e `/privacidade`; `SignupDetails.tsx` aponta para documentos acessiveis; typecheck web passou. | Manter textos legais acessiveis antes do aceite. | Usuario consegue ler textos legais antes do aceite. |
| AUD-P1-030 | Politica de privacidade contradiz o produto sobre conversas e compartilhamento com terceiros. | 6 | Alta | P1 | Alta | Em mitigacao | Landing privacidade foi suavizada para nao negar armazenamento/compartilhamento de forma absoluta; typecheck landing passou. | Revisar politica com dados, subprocessadores, bases legais e retencao reais. | Politica publica bate com arquitetura e PRD aprovados. |
| AUD-P1-031 | Landing/LGPD fazem claims absolutos acima da evidencia atual de auditoria. | 6 | Alta | P1 | Alta | Em mitigacao | Claims publicos de LGPD/RLS/criptografia foram suavizados enquanto P0 RLS permanece aberto; typecheck landing passou. | Remover claims absolutos ate haver prova DB-backed e aceite legal. | Claims publicos sao proporcionais a evidencia aprovada. |
| AUD-P1-032 | Termos sao insuficientes para uso aceitavel, spam, WhatsApp/Meta, quotas, SLA e inadimplencia. | 6 | Alta | P1 | Alta | Em mitigacao | Termos publicos foram ajustados para linguagem mais conservadora, mas ainda precisam revisao juridica formal. | Expandir termos com governanca comercial e uso aceitavel. | Termos cobrem riscos operacionais e comerciais do produto. |
| AUD-P2-033 | Direitos LGPD existem no contrato, mas nao aparecem como UX operacional no app. | 4/6 | Media | P2 | Media | Aberto | OpenAPI documenta export/delete; Settings nao expoe fluxo operacional. | Criar UX ou canal manual/SLA dentro do produto. | Usuario ve como solicitar exportacao, exclusao, correcao e opt-out. |
| AUD-P2-034 | Admin Tenants/Templates nao tem estados persistentes de loading/vazio/erro. | 4 | Media | P2 | Media | Aberto | Falhas geram toast transitorio e listas vazias. | Adicionar estados estruturais e acoes de retry. | Operador distingue vazio real de falha de API. |
| AUD-P3-035 | Problemas aparentes de a11y/polimento em contraste, animacoes e rotulos. | 4 | Baixa | P3 | Media | Aberto | Exemplos em Settings/Admin Login por inspecao estatica. | Revisao a11y com foco, contraste, labels e reducao de animacoes. | Auditoria visual/browser nao encontra bloqueios basicos. |

## Regras de uso

- Nunca remover achado sem registrar criterio de aceite.
- Para marcar `Resolvido`, incluir PR/commit ou evidencia equivalente.
- Para marcar `Aceito com risco`, preencher [template-aceite-risco.md](template-aceite-risco.md), incluir aprovador Gustavo, data, escopo, prazo de revisita e revisao Claude quando severidade for Alta ou Critica.
- Achados P0 bloqueiam merge/release.
- Achados P1 bloqueiam go-live e podem bloquear merge se tocarem area critica.
